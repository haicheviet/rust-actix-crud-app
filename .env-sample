DB_USER=demo
DB_PASSWORD=demo
DB_NAME=demo
DB_HOST=127.0.0.1
DB_PORT=5432
# Specific DATABASE_URL variable name required for sqlx-prepare
DATABASE_URL="postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"
ACTIX_PORT=8000